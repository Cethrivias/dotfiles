{
    "fori": {
        "prefix": "fori",
        "body": [
            "for (let ${1:i} = 0; ${1:i} < ${2:array}.length; ${1:i}++) {",
            "   const it = ${2:array}[${1:i}]",
            "   $0",
            "}"
        ]
    }
}
